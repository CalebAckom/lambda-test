{
  "name": "lambda-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "lambda-build upload url-to-html -r eu-west-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Caleb Poku Ackom",
  "license": "ISC",
  "devDependencies": {
    "aws-lambda": "^1.0.7",
    "aws-lambda-build": "^1.0.8"
  }
}
